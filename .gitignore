# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (NEVER commit these!)
.env
.env.local
.env.production
.env.development
.env.firebase

# Firebase credentials (NEVER commit these!)
firebase-service-account.json
.firebase/

# Migration data
migration-data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Release AAB files
*.aab

# Android build artifacts
plugins/*/android/build/

# Build/deploy scripts with embedded secrets (NEVER commit — contain keystore password)
build_aab_*.bat
build_cap_gradle_*.bat
commit_*.bat
do_commit.bat

# Build/deploy logs
build_*_out.txt
build_*_err.txt
cap_sync_*.txt
deploy_*_out.txt
deploy_*_err.txt
commit_*_out.txt

# Vite build cache files (auto-generated)
vite.config.ts.timestamp-*.mjs

# Stray dev artifacts (review before committing)
generatePalette()
semantic

